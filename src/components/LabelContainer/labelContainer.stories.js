import React from "react";
import LabelContainer from "./";

export default {
  title: "UI Components/LabelContainer",
  component: LabelContainer,
};

export const Default = () => {
  return <LabelContainer>Default</LabelContainer>;
};
