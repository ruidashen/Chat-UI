import React from "react";
import FilterList from "./";

export default {
  title: "Page Components/FilterList",
  component: FilterList,
};

export const Default = () => {
  return <FilterList>Add list here</FilterList>;
};
