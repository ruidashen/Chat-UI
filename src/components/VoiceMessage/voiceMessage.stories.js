import React from "react";
import VoiceMessage from "./";

export default {
  title: "UI Components/VoiceMessage",
  component: VoiceMessage,
};

export const Default = () => {
  return <VoiceMessage time="01:25"></VoiceMessage>;
};
